# CLAUDE.md — Start Here

This is the base file for this project. **Always read this file first**, at the start of every session, before touching anything else.

## What to do after reading this file

1. Read `project.json` — full project data: structure, files, and the functions they contain. Use it as an index to find where something lives.
2. Read `action-map.json` — the full history of actions taken against user requirements, so past work isn't repeated or lost.
3. Do **not** read entire project files by default. Use `project.json` to locate the specific file/function you need, then read only that.
4. If a task touches a feature, check `features/` for its md file before starting — it may already document current behavior, open issues, or prior decisions.
5. If a task is similar to something done before, check `knoladge/` for how it was implemented last time (including any past failures) before redoing the research.

## Update rules (do these every time you make a change)

- **action-map.json**: append an entry for every action taken, tied to the user requirement that triggered it.
- **project.json**: update whenever files/functions are added, removed, or change purpose.
- **features/\<feature\>.md**: create one file per complete feature (not per small edit). Update the relevant feature file whenever that feature changes. A feature file should describe the whole feature, not just the latest tweak.
- **knoladge/**: document how something was implemented and what was learned, so it can be reused next time. Document failures and errors too — what went wrong and how (or whether) it was resolved.
- **Theme version**: every time you change anything in the theme (code, styling, assets — not just this doc system), bump the version in **all three** places that hold a copy of it: `style.css`'s `Version:` header, `functions.php`'s `HEX_VERSION` constant, and `tests/bootstrap.php`'s `HEX_VERSION` define (the test suite runs without loading `functions.php`, so it keeps its own copy — `tests/ThemeFilesTest.php` will fail if this one drifts from `style.css`). A docs-only change (editing `claude.md`/`project.json`/`action-map.json`/`features/`/`knoladge/` with no theme file touched) doesn't need a bump.
- **Scope discipline — main theme vs. child theme**: when the user's request is explicitly scoped to "the child theme" or "the main theme" (either by name or by clear context), confine all changes to that scope only. Don't touch the other one's files, settings, options, or behavior as a side effect — e.g. a child-theme-scoped request shouldn't modify `inc/updater.php`, the main `hex_updates` settings group, or any front-end/main-admin file, and vice versa. If a change genuinely needs to touch both, say so explicitly before doing it rather than silently expanding scope.

## Boundaries & rules

*(Populated as the user gives instructions. Keep this section current — it is the authoritative list of constraints for this project.)*

- **STRICT FOLDER BOUNDARY**: all work — reads, writes, edits, deletes, and any MCP filesystem ability (`novamira/read-file`, `write-file`, `edit-file`, `delete-file`, `list-directory`, `execute-php` touching files, etc.) — is confined to this theme's own directory (`wp-content/themes/hex-wp-theme-template/` and its subfolders) and nothing outside it. Never read, write, or delete anything elsewhere on the site or filesystem (other themes, plugins, `wp-config.php`, WordPress core, `wp-content/uploads`, etc.) unless the user explicitly names that path in a specific request. This rule is strict and does not get relaxed by convenience.
  - **Exception scope, not a contradiction**: this constrains *my own* tool usage while developing, not what the *theme's own code* is allowed to do at runtime on the site owner's behalf. The child-theme feature (`inc/child-theme.php`, see `features/child-theme.md`) legitimately writes a new theme directory under `wp-content/themes/` when the site admin clicks "Fetch & Install Child Theme" — that's normal WordPress behavior, not a boundary violation. What the boundary *does* forbid: me personally invoking that code (or otherwise writing outside this folder) during a session to "test" or "verify" it, without the user explicitly asking for that specific action.
- **Main-theme and child-theme updates are, and must stay, fully isolated**: separate option names (`hex_github_repo`/`hex_github_branch`/`hex_activation_key` vs. `hex_child_github_repo`/`hex_child_github_branch`/`hex_child_activation_key`), separate `PucFactory` checker instances with separate `static` caches (`hex_get_update_checker()` vs. `hex_get_child_update_checker()`), separate settings groups (`hex_updates` vs. `hex_child_updates`), separate transient logs (`hex_updates_log` vs. `hex_child_theme_log`), and `Theme_Upgrader->upgrade()`/`->install()` always called with an explicit, correct slug (`get_template()` for the main theme, the stored child slug for the child). If main-theme has an update, only the main theme is checked/installed; if the child theme has an update, only the child theme is checked/installed — never both from one action. Any future change to the updater/child-theme code must preserve this: don't introduce a shared checker, a shared option, or a code path where checking/updating one could affect the other.
- **Use MCP whenever it applies**: when a task needs live WordPress state, DB data, running PHP, WP-CLI, Gutenberg block registry info, or the active design system, use the `wordpress-local` MCP abilities instead of guessing or hand-simulating WordPress behavior. Still respect the folder boundary above when doing so.
- **Code quality**: follow WordPress Coding Standards (WPCS via PHPCS, `phpcs.xml.dist`) and general industry-standard PHP practice — escape all output, sanitize/validate all input, nonce-protect state-changing actions, prefix every global function/constant (`hex_` / `HEX_`) to avoid collisions. Run `vendor/bin/phpcs --standard=phpcs.xml.dist` before considering PHP work done.
- **Comments**: professional and minimal — a short docblock per function/file, inline comments only where the *why* isn't obvious from the code. No comment noise.
- **Testing**: every feature needs test coverage. Add/extend PHPUnit tests (`tests/*Test.php`, WP_Mock-based — no live WP DB required) for new logic, and run `vendor/bin/phpunit` before considering PHP work done.
- **This theme's versioning rule**: `style.css`'s `Version:` header and `functions.php`'s `HEX_VERSION` constant must always match. Enforced automatically by `tests/ThemeFilesTest.php`.
- The theme's exactly-three-templates contract is enforced by `hex_get_page_templates()` in `inc/setup.php` (single source of truth) and checked against the actual files by `tests/ThemeFilesTest.php`. Don't add a fourth template or rename the existing ones without updating both.
- **Styling**: Tailwind CSS (v4) + Animate.css on both front end and admin, compiled via `npm run build:css` (two separate builds — see `features/brand-styling.md`). Avoid hand-written CSS; express styling as Tailwind utility classes in markup, and only add custom CSS for something utilities genuinely can't express (documented inline when it happens).
- **MANDATORY when building or styling any front-end page/template/component**: use the design-system classes/components from `features/design-system.md` (bare `<h1>`–`<h6>` — already fully tokenized; `text-lead`/`text-large`/`text-small`/`text-meta`; the `p-xs`…`p-2xl` + `py-section-*` spacing scale; `bg-site-primary`/`text-site-*`/etc.; `.btn` + variants; `.card`; `.form-control`+`.form-label`; `.alert`+variants; `.badge`+variants; `.table-styled`; `.icon`) instead of raw Tailwind equivalents or hand-rolled markup (`text-3xl`, `p-8`, `bg-blue-600`, a hand-rolled `<div>` for an alert, `[&_input]:...` arbitrary variants for a form field) — read that doc's tables before styling anything. Raw Tailwind values never respond to the Theme Options admin page; design-system classes always do. Raw utilities are still fine for pure layout/structure (`flex`, `grid`, one-off `gap-*` on non-content elements, responsive prefixes). Exception to know about: `rounded-sm`/`rounded-md`/`rounded-lg` are deliberately NOT left alone — the Global radius settings override them site-wide on purpose.
- **The Hexnity logo/brand color palette (`hexnity-*` in `assets/css/src/theme.css`) is admin-only** — explicit user correction: *"just use logo and colors only for admin pages. dont use it for theme."* The front-end build (`assets/css/src/front.css`) doesn't even import `theme.css`. Don't reintroduce the logo or `hexnity-*` classes to any front-end template without the user asking again.
  - Within the admin itself, `hexnity-*` is **not mandatory on every element** — explicit follow-up: *"u dont need to strict with logo colors."* The logo/`hexnity-900` sidebar background is the one place brand identity matters; everywhere else in the dashboard (buttons, links, active states, focus rings) uses Tailwind's `indigo` scale as the general accent. Don't force `hexnity-*` onto new admin UI just for brand consistency — pick whatever reads well, same as any other admin design decision.
- **Tailwind-in-wp-admin color gotcha**: WordPress core's own admin CSS is unlayered, and Tailwind puts every utility in a `@layer` — unlayered rules always beat layered ones regardless of specificity, so a plain Tailwind color utility (e.g. `text-white`) can silently lose to WP core's own `a { color: ... }`. If an admin-page color utility doesn't visually apply, this is almost certainly why — fix with Tailwind v4's trailing `!` important-modifier (`text-white!`), and verify `!important` actually landed in the compiled CSS before calling it fixed. See `knoladge/tailwind-cascade-layers-vs-wp-admin.md`.

### github.md's relationship to this theme

- `github.md` documents a **different** project's (`shashinthalk/chindfundwptheme`) self-updater, including a *hardcoded* token and a hardcoded repo. **This theme's own updater (`inc/updater.php`) follows the same architecture but nothing is hardcoded** — the user explicitly asked for the GitHub repo, branch, and access token to all be admin-configurable (Updates page), not baked into the source. Don't hardcode a repo or token here even by analogy to `github.md` — that project's specific hardcoding choice does not carry over.
- The `inc/lib/plugin-update-checker/` path convention (theme-owned path, not the top-level `vendor/`) **does** carry over — same reasoning, same fix. See `features/github-updater.md`.

## Known project state (2026-08-27)

A full classic (non-block) theme has been built from scratch in this folder. The user renamed it directly in `style.css` to **"Hexnity WP AI Theme"** (author: Nishan Shashintha) — the admin UI reads this name live via `wp_get_theme()`, so further renames need no code change. Current feature set:

- Theme bootstrap (`functions.php` + `inc/`), all required templates (`header.php`, `footer.php`, `index.php`, `page.php`, `single.php`, `archive.php`, `search.php`, `404.php`, `comments.php`), and exactly three page templates (`template-default.php`, `template-full-width.php`, `template-canvas.php`). See `features/theme-foundation.md`, `features/page-templates.md`.
- A GitHub-based self-updater (`inc/updater.php`, vendored `yahnis-elsts/plugin-update-checker` at `inc/lib/`) — admin-configured repo/branch/token, no repo configured yet. See `features/github-updater.md`.
- A theme management admin dashboard (`inc/admin/`) — Dashboard, Updates, Child Theme, Theme Options, and About pages under one top-level menu (sidebar label is the fixed string **"Hexnity WP"**, deliberately not the full theme name — see `inc/admin/menu.php`), presented as a fully dark app-shell: a persistent dark Hexnity-branded sidebar (logo, icon nav, version footer) plus an equally dark content pane with its own header bar, shared by all five pages via `hex_render_admin_shell_start()`/`_end()` in `inc/admin/partials.php`. Dashboard leads with a 4-tile stat row. Buttons/inputs use a dedicated component layer (`.hex-btn` variants, `.hex-field`) in `assets/css/src/admin.css`, and the general interactive accent color is Tailwind's `indigo` scale, not the `hexnity-*` brand palette (that's confined to the sidebar only — user: *"u dont need to strict with logo colors"*). See `features/theme-admin-dashboard.md`, `knoladge/admin-dark-theme-components.md`.
- A child theme is **fetched and validated from GitHub, not auto-generated** (per explicit user correction: *"child theme is not create automatically... it should ask valid childtheme url and it should validate if that is actual child of this parent, then it should download"*) — `inc/child-theme.php` fetches the saved repo's `style.css` from `raw.githubusercontent.com`, confirms its `Template:` header matches this theme's own slug, and only then installs via `Theme_Upgrader->install()`. It then has its own fully independent GitHub self-updater (separate repo/branch/token, separate checker instance from the main theme's). No child theme installed yet. See `features/child-theme.md`.
- Tailwind CSS v4 + Animate.css styling on both front end and admin (two separate compiled builds — front end has Preflight, admin doesn't, to avoid resetting the rest of wp-admin's chrome). Front end is neutral gray, no brand color/logo. See `features/brand-styling.md`.
- A full admin-configurable design-token system (Theme Options page, tabbed UI) — ~146 fields across 12 groups: Typography (H1-H6 incl. line-height/letter-spacing/weight/margin, body/lead/large/small/meta, font families, link colors), Spacing, Colors (15-color palette), Buttons (4 variants + text/outline), Forms, Cards, Sections, Global radius, Tables, Alerts, Badges, Icons — applied site-wide via runtime CSS custom properties (no rebuild needed to see a change). Editing is gated on an active child theme; the values themselves always apply regardless. Every font-family field has a searchable `<datalist>` sourced from an admin-configurable, API-free Google Fonts picker (paste a Google Fonts embed link; `inc/google-fonts.php` parses the family name(s) out of it and enqueues the stylesheet on the front end) — see `knoladge/google-fonts-picker.md`. **Read `features/design-system.md` before styling anything front-end** — it has the required class-to-use tables. See `inc/style-settings.php`, `assets/css/src/site-theme.css`.
- Dev toolchain: Composer (PHPCS/WPCS + PHPCompatibilityWP, PHPUnit + WP_Mock) and npm (Tailwind + Animate.css + Typography plugin, dev-only — compiled CSS is committed since WordPress can't run an npm build in production). See `features/testing-and-tooling.md`.

- Run tests: `composer install && vendor/bin/phpunit` (103 tests passing as of 2026-08-27).
- Run the linter: `vendor/bin/phpcs --standard=phpcs.xml.dist` (0 errors/warnings as of 2026-08-27).
- Rebuild CSS after any template/class change: `npm install && npm run build:css`.
- The theme has **not** been activated/viewed on the live site yet — only unit-tested, linted, and CSS-build-verified so far.
- The GitHub updater has **no repository configured yet** — it has never run against a real GitHub API. Ask the user for a repo (and, if private, a token) before claiming the updater "works" end-to-end.
- No child theme has actually been installed via `hex_install_child_theme_from_repo()` (its `Theme_Upgrader`/`WP_Filesystem` path is still untested live) — but a hand-written, ready-to-push starter child theme was created at `../hexnity-wp-child/` (a sibling of this theme's folder, explicitly requested by the user) so they can push it to their own GitHub repo and exercise the fetch-and-install feature end-to-end once it exists there. See `action-map.json`'s last entry for the compatibility reasoning (it deliberately doesn't use the generic "enqueue parent style.css" boilerplate, since this parent's real styling is the compiled Tailwind CSS, not style.css). That sibling directory is **not** part of this project's own doc index (project.json) — it's a separate theme, tracked here only as a historical note.
- Current version: **1.5.0** (bumped per the version-bump rule above; see `action-map.json` for what each bump corresponded to).
